# +----------------------------------------------------------------------
# | @project   本心通用管理后台 / BenxinAdminPro
# | @mission   环境变量示例 — 仅覆盖少量运行时开关，主配置见 server/config/config.example.yaml
# | @author    仗键天涯(daxing)
# | @email     3442535897@qq.com
# | @date      2026-06-07 15:45:00
# +----------------------------------------------------------------------
#
# 应用配置以 YAML 文件为主：server/config/config.yaml（从 config.example.yaml 复制后填入真实值）。
# 环境变量可覆盖 YAML 中的同名键（前缀 BENXIN_，下划线分隔层级）。
#
# 示例：
#   BENXIN_CRYPTO_AES_KEY_B64=<base64 编码的 32 字节密钥>
#   BENXIN_JWT_ACCESS_SECRET=<强随机字符串>
#   BENXIN_JWT_REFRESH_SECRET=<强随机字符串>
#   BENXIN_MYSQL_DSN=root:root@tcp(localhost:3306)/benxinadminpro?charset=utf8mb4&parseTime=true&loc=Local
#   BENXIN_REDIS_ADDR=localhost:6379
#
# 密钥生成：openssl rand -base64 32
#
# 禁止将真实密钥提交到 git 仓库。本文件仅为格式说明。
