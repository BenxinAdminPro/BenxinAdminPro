---
description: BenxinAdminPro 底座项目规则（全局始终生效）
alwaysApply: true
---

# BenxinAdminPro（本心通用管理后台）— Cursor 规则

> 放置：`BenxinAdminPro/.cursor/rules/benxinadminpro.mdc`
> BenxinAdminPro 是开箱即用的完整后台系统，同时是纯 Go 的可复用底座（脚手架 + 模块库）。本规则约束"底座必须业务中立、可参数化复用"。

## 0. 这是底座，不是业务应用
- 只放与具体业务无关、每个后台应用都用得上的能力：认证、RBAC、系统管理、配置中心、加密中间件、统一响应、限流、监控、代码生成、消息中心、驱动接口。
- 判断标准：写任何代码前自问"下一个完全不同的应用还用得上吗？"——用不上不要写进来。
- **禁止任何业务概念**：内容/文章/音视频/直播/会员/积分/支付/订单/优惠券/评论/客服等一律不进。

## 1. 参数化铁律（最重要）
**禁止硬编码任何应用专属常量**：
- 表前缀用配置项（如 `cfg.TablePrefix`），禁止写死 `kp_`。
- Redis key 前缀用配置项（如 `cfg.RedisNamespace`），禁止写死 `okp:`。
- 错误码：底座占约定段（如 1000-1999），业务段留消费方。
- 加密主密钥经环境变量注入，禁止内置默认密钥。

## 2. 复用机制
- 通过带 SemVer 的 Go module（`benxinadminpro-server`）/ npm 包复用，非复制粘贴。
- 应用差异通过接口/扩展点/配置注入，绝不为某应用改底座源码。
- `v1.x` 内向后兼容；破坏性变更升 `v2`。

## 3. 纯 Go + 语言中立 spec
- 底座只写 Go。不写 PHP。
- 同时维护 `server/spec/`（OpenAPI / SQL DDL / casbin model.conf / 加密向量），供其它语言（如 BenxinKP 的 PHP 后端）实现 parity。

## 4. 技术栈
Go 1.26 + Gin；GORM + gorm/gen；Casbin；go-redis；golang-jwt；viper；zap；validator；swaggo/swag；gopsutil；ulule/limiter。前端：Vue3 + Element Plus + Vite + TS + UnoCSS + Pinia。

## 5. 安全与规范（宪法级）
- 安全第一，服务端唯一权威，永不信任前端。
- 加密：C 端传输 AES-256-CBC+PKCS7+随机IV前缀+HMAC-SHA256+timestamp/nonce 防重放；配置 at-rest AES-256-GCM。向量落 `server/spec/crypto-vectors/`。
- 密码 Argon2id；JWT 访问+刷新+jti 黑名单（Redis）。
- DB：金额存分、统一时间字段、软删除、对外 ID 用 Hashid(加盐)/UUIDv7；DDL 走 `server/spec/migrations/` 纯 SQL，禁 AutoMigrate。
- SQL 参数化、输出转义、配置 URL 防 SSRF；安全响应头固定值。
- 仅用开源/可商用素材（前端侧）。

## 6. 代码头注释（每个新建文件必须带，五项）
①项目名称中英文 ②本文件功能/模块 ③作者 ④邮箱 ⑤生成日期时间（到秒）。
```
// +----------------------------------------------------------------------
// | @project   本心通用管理后台 / BenxinAdminPro
// | @mission   本文件功能或模块说明 — API 控制器附 HTTP 方法与路由
// | @author    仗键天涯(daxing)
// | @email     3442535897@qq.com
// | @date      2026-06-05 22:00:00
// +----------------------------------------------------------------------
```
- `@date` 取文件生成当时的 `YYYY-MM-DD HH:mm:ss`；修改既有文件追加 `@updated YYYY-MM-DD HH:mm:ss`。

## 7. 交付与测试
- Cursor 负责：编码 + API/单测/契约测试 + git 提交（双仓库 Gitee 主 + GitHub 镜像）。
- daxing 负责：用 `server/examples/demo` 真人验收 + 关键决策。
- 每个能力须在 demo 跑起来、可演示。完成后输出可复制 markdown 完成报告。

## 8. 本机环境
docker-compose 仅起依赖（MySQL/Redis），server/admin/demo 原生跑；连 localhost:3306/6379。生产不容器化。

## 9. git 红线
禁止提交密钥/IP/证书/.env 到任一仓库；提交前确认 .gitignore 覆盖 .env/*.key/*.pem/*.crt/vendor//node_modules/。
